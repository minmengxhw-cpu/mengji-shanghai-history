import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Cloudflare/vinext is the primary production target. GitHub Pages uses
     the prerendered HTML snapshot assembled by scripts/build-pages.sh. */
};

export default nextConfig;
